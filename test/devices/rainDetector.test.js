import RainDetector from "../../src/devices/rainDetector";
import {expect} from "chai";

describe("RainDetector", function () {
    it("should be defined", function () {
        expect(RainDetector).to.exist;
    });
});