import Switcher from "../../src/devices/switcher";
import {expect} from "chai";

describe("Switcher", function () {
    it("should be defined", function () {
        expect(Switcher).to.exist;
    });
});
